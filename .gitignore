# Node
node_modules/
dist/
build/

# Next.js
.next/

# Env
.env
.env.local
.env.*.local

# Python
venv/
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Backend runtime data (regenerate the model with `python -m app.ml.training`)
backend/data/*.db
backend/data/*.db-journal
backend/data/chain.json
backend/data/*.joblib
backend/data/model_metrics.json
backend/data/smoke*

# Logs
logs/
*.log

# OS
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo

# Markdown docs (architecture, audit report, plans, summaries — kept local only)
*.md
# …except the main README at the repo root, which we do publish
!/README.md

# Claude Code workspace
.claude/
